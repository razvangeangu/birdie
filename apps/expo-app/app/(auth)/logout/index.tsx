import { Typography } from '@birdie/design-system';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Page() {
  return (
    <SafeAreaView>
      <Typography>Logout</Typography>
    </SafeAreaView>
  );
}
